<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { title: 'Hero Vault'}) %>
<body>
  <%- include('partials/header') %>
  <section>
    <ul>
      <% heroes.forEach(hero => { %>
        <li>
          <a href="/categories/<%= category_id %>/<%= hero.hero_id %>">
            <%= hero.name %>
          </a>
        </li>
      <% }); %>
    </ul>
  </section>
  <%- include('partials/footer') %>
</body>
</html>